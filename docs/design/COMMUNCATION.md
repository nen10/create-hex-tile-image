
- ユースケースに対して skill として記述するべき要件と、そこから推測される、開発項目として記述するべき要件とを区別して、作業を進めてください。
- 一つのtoolではなく、複数のtoolを作成し skill で連携させることを考慮して設計します


擦り合わせ項目:


> 1. agent は **ローカル CLI** として使う想定？それともブラウザ内の JS API として使う想定？

-> ユースケースは 
1-a. agent 自身が Image 2.0 を使用して何か画像を作成 -> tool を使って Hex 形状に crop することです。
作業の際、crop 直前の生成画像も残します。
出力形式( 出力サイズ、Pointy-top, Flat-top ) の指定は、作業 pipeline 外で需要される formatから 判断の余地があります。後続の作業のため atlas 画像としてまとめる単位及び出力形式によってディレクトリを作成し出力先指定します。
他の指定項目...高レベル指定等は、内部的な観点でそのとき定めます。

1-b. crop後の複数の画像をまとめたdirectoryに対して、 tile atlas 画像としてまとめる。異なるサイズの画像, Pointy-top, Flat-top, 不要な画像を混ぜない。Godot 向けの tile atlas 画像は、h x w が abs(h - w) を 小さくするように並べたいですね。

> 2. agent が crop 位置を決める時、画像を見て `x,y,w,h` を渡す想定？それとも「中央」「顔中心」「全体フィット」みたいな高レベル指定が主？

-> 高レベル指定「中央」「顔中心」「全体フィット」で進めましょう

> 3. batch 処理は必要？単発 crop だけで十分？

-> 必要だと思います

> 4. 出力は PNG だけでいい？manifest JSON と preview overlay は最初から欲しい？

-> 作業方法から必要を決めてください。

